{
  "name": "lwill-ai-builder",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "argon2"
    ]
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "turbo run format",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
